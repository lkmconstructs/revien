pytest>=7.4.0
pytest-asyncio>=0.21.0
# MCP surface (LEG P5). NOTE: install alongside fastapi<0.116 so the resolver
# picks sse-starlette<2.3 (newer sse-starlette needs starlette>=0.49, which
# conflicts with fastapi 0.115's starlette<0.42 pin). A fresh resolve handles
# this; upgrading mcp in-place may drag starlette past fastapi's pin.
mcp>=1.28.1
